---
import { ui, type Lang } from '../lib/i18n';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = ui[lang];
---

<section id="com-treballem" class="grain-inset relative scroll-mt-24 overflow-hidden bg-pine py-24 text-bg md:py-32">
  <div class="container-edge relative z-10">
    <div class="max-w-2xl">
      <p class="eyebrow border-white/10 bg-white/5 text-coral" data-reveal>{t.process.eyebrow}</p>
      <h2 class="mt-5 text-h2 font-semibold text-bg" data-reveal>{t.process.title}</h2>
      <p class="mt-5 text-lg text-bg/70" data-reveal>{t.process.lead}</p>
    </div>

    <!-- Sticky stack (GSAP escala les targetes anteriors en scroll) -->
    <div class="relative mt-16" data-stack>
      {
        t.process.steps.map((step) => (
          <article
            class="sticky top-28 mb-5 origin-top overflow-hidden rounded-2xl border border-white/10 bg-pine-soft p-7 md:p-10"
            data-stack-card
            style={`box-shadow: 0 -8px 40px -20px rgb(0 0 0 / 0.6);`}
          >
            <div class="grid gap-6 md:grid-cols-[auto_1fr] md:items-start md:gap-10">
              <span class="font-display text-5xl font-semibold text-accent md:text-7xl">{step.n}</span>
              <div class="max-w-2xl">
                <h3 class="font-display text-2xl font-semibold text-bg md:text-3xl">{step.title}</h3>
                <p class="mt-3 text-bg/70 md:text-lg">{step.body}</p>
                <div
                  class="mt-6 h-px w-full bg-gradient-to-r from-accent/60 to-transparent"
                  aria-hidden="true"
                />
              </div>
            </div>
          </article>
        ))
      }
    </div>

    <p class="mt-10 max-w-xl text-sm text-bg/55" data-reveal>
      {
        lang === 'ca'
          ? 'En cap pas es publica res sense que una persona ho hagi revisat i tu ho hagis vist. Aquesta és la diferència amb un bot.'
          : 'En ningún paso se publica nada sin que una persona lo haya revisado y tú lo hayas visto. Esa es la diferencia con un bot.'
      }
    </p>
  </div>
</section>
