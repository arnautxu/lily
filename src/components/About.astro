---
import { ui, type Lang } from '../lib/i18n';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = ui[lang];
---

<section id="lidia" class="scroll-mt-24 section">
  <div class="container-edge grid items-center gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:gap-16">
    <!-- Foto de la Lídia (double-bezel) -->
    <div class="relative mx-auto w-full max-w-sm lg:mx-0" data-reveal>
      <div class="bezel">
        <div class="bezel-core aspect-[4/5] overflow-hidden bg-gradient-to-br from-[#5d7d3a] via-[#4d7c2f] to-[#385c22]">
          <img
            src="/lidia.jpeg"
            alt={t.about.signature}
            width="908"
            height="1124"
            loading="lazy"
            class="h-full w-full object-cover"
            onerror="this.style.display='none'"
          />
        </div>
      </div>
    </div>

    <div class="max-w-xl">
      <p class="eyebrow" data-reveal>{t.about.eyebrow}</p>
      <h2 class="mt-5 text-h2 font-semibold" data-reveal>{t.about.title}</h2>
      {t.about.body.map((p) => <p class="lead mt-4" data-reveal>{p}</p>)}

      <div class="mt-8" data-reveal>
        <p class="font-display text-lg font-semibold">{t.about.signature}</p>
        <p class="text-sm text-ink-soft">{t.about.role}</p>
      </div>
    </div>
  </div>
</section>
